import React from 'react';
import { View, Text } from 'react-native';

const Scan = () => {
  return (
    <View className="flex flex-col h-screen">
      <Text className="text-black font-bold text-lg shadow-lg">Scan</Text>
    </View>
  );
};

export default Scan;
